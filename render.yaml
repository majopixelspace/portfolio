# Render Blueprint — static site (no build step)
# Docs: https://render.com/docs/blueprint-spec
services:
  - type: web
    name: majo-portfolio
    runtime: static
    buildCommand: ""
    staticPublishPath: .
